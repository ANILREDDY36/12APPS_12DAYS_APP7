<!DOCTYPE html>
<html>
  <head>
    <title>App7</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-brand">Rails Jobs</div>
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "All Jobs", root_path %>
        </li>
        <% Category.all.each do |category| %>
          <li>
            <%= link_to category.name, jobs_path(category: category.name) %>
          </li>
        <% end %>
      </ul>
      <%= link_to "New Job", new_job_path, class: "navbar-text navbar-right navbar-link" %>
      <%= link_to "New Category", new_category_path, class: "navbar-text navbar-right navbar-link" %>
    </div>
  </nav>
  <div class="container">
    <div class="col-md-6 col-md-offset-3">
      <%= yield %>
    </div>
  </div>
  </body>
</html>
